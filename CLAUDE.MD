# Назначение проекта

Telegram-бот для автоматической синхронизации данных товаров между тремя маркетплейсами: Wildberries, Ozon и Яндекс.Маркет. Система использует AI для интеллектуального сопоставления столбцов Excel-файлов и валидации значений с учётом справочников (validation lists).

## Архитектура

- **Bot Framework**: aiogram 3.x (async)
- **База данных**: SQLite (marketplace_sync.db)
- **AI Integration**: OpenRouter API (настраиваемые модели через .env)
- **File Processing**: pandas + openpyxl
- **State Management**: FSM (Finite State Machine)
- **Access Control**: Многоуровневая система прав доступа

## Структура проекта

```
/
├── main.py                          # Точка входа приложения
├── .env                             # Конфигурация (API ключи, ID владельца/админа)
├── marketplace_sync.db              # База данных SQLite
│
├── /bot/
│   ├── bot.py                       # Инициализация бота и диспетчера
│   ├── storage.py                   # Глобальный экземпляр Database
│   ├── security.py -> access.py     # Псевдоним для AccessManager
│   │
│   ├── /handlers/                   # Обработчики команд бота
│   │   ├── common.py                # Общие команды (/start, меню)
│   │   ├── upload.py                # Загрузка и обработка файлов
│   │   ├── schema_create.py         # Создание схем сопоставлений
│   │   ├── schema_edit.py           # Редактирование схем
│   │   ├── schema_update.py         # Обновление существующих схем
│   │   ├── schema_delete.py         # Удаление схем
│   │   ├── stats.py                 # Статистика пользователя
│   │   └── access_management.py     # Управление доступами (владелец/админ)
│   │
│   ├── /middlewares/
│   │   └── access_control.py        # Middleware проверки прав доступа
│   │
│   ├── keyboards.py                 # Все клавиатуры бота
│   ├── states.py                    # FSM состояния
│   └── access.py                    # AccessManager - управление правами
│
├── /services/
│   ├── data_synchronizer.py         # Основная логика синхронизации данных
│   └── ai_comparator.py             # AI-сопоставление столбцов
│
├── /utils/
│   ├── excel_reader.py              # Чтение Excel файлов
│   ├── excel_writer.py              # Создание отчётов Excel
│   ├── logger_config.py             # Конфигурация логирования
│   └── utils.py                     # Вспомогательные функции
│
├── /database/
│   └── database.py                  # SQLite ORM и миграции
│
└── /config/
    └── config.py                    # Конфигурация (API keys, маппинги)
```

## Система прав доступа

### Иерархия ролей

1. **👑 Владелец (Owner)** - 1 слот
   - ID задаётся в `.env` (`ACCESS_OWNER_ID`)
   - Полный контроль над ботом
   - Управление администратором
   - Управление белым списком (whitelist)
   - Доступ ко всем схемам (своим и чужим)

2. **👨‍💼 Администратор (Admin)** - 1 слот (опционально)
   - Назначается владельцем через БД или `.env` (`ACCESS_ADMIN_ID`)
   - Управление белым списком пользователей
   - Доступ ко всем схемам
   - НЕ может управлять владельцем

3. **✏️ Редактор (Editor)** - 1 слот в whitelist
   - Добавляется через меню управления доступами
   - Доступ ко всем схемам (просмотр и редактирование)
   - Создание/редактирование/удаление СВОИХ схем
   - Загрузка и обработка файлов
   - НЕ может управлять доступами других пользователей

4. **👤 Пользователь (User)** - 2 слота в whitelist
   - Базовый доступ к боту
   - Видит ТОЛЬКО свои схемы
   - Создание/редактирование/удаление СВОИХ схем
   - Загрузка и обработка файлов
   - Просмотр статистики

### Whitelist

- **Лимиты**: Отсутствуют
  - Неограниченное количество редакторов (editor)
  - Неограниченное количество пользователей (user)
- **Управление**: Только владелец и администратор
- **Проверка прав**: AccessControlMiddleware на уровне dispatcher

### AccessManager (bot/access.py)

Центральный класс для проверки прав доступа:

```python
# Основные методы
AccessManager.has_access(user_id)                    # Есть ли доступ к боту
AccessManager.has_access_management_rights(user_id)  # Может ли управлять доступами
AccessManager.is_owner(user_id)                      # Является ли владельцем
AccessManager.is_admin(user_id)                      # Является ли администратором
AccessManager.is_editor(user_id)                     # Является ли редактором
AccessManager.can_see_all_schemas(user_id)           # Видит ли все схемы
AccessManager.can_manage_schemas(user_id)            # Может ли управлять схемами
```

## База данных

### Таблицы

```sql
users                   # Пользователи бота
  - user_id (PK)
  - username, first_name, last_name
  - registered_at
  - total_processings

processing_history      # История обработок файлов
  - id (PK)
  - user_id (FK)
  - started_at, completed_at
  - wb_products_count, ozon_products_count, yandex_products_count
  - synced_cells_count
  - status, error_message

files                   # Загруженные файлы
  - id (PK)
  - user_id (FK), processing_id (FK)
  - marketplace, original_filename, file_path
  - uploaded_at

schemas                 # Схемы сопоставлений
  - id (PK)
  - user_id (FK)
  - schema_name (UNIQUE per user)
  - created_at, updated_at
  - full_comparison_json (TEXT) - полный результат AI

schema_matches          # Тройные совпадения (legacy, для совместимости)
  - id (PK), schema_id (FK)
  - wb_column, ozon_column, yandex_column
  - confidence, is_mandatory

system_settings         # Системные настройки
  - setting_key (PK)    # Например: 'admin_user_id'
  - setting_value
  - updated_at, updated_by

whitelist_users         # Белый список (макс. 3)
  - id (PK)
  - user_id (UNIQUE)
  - role (TEXT)         # 'editor' или 'user'
  - added_at, added_by
  - notes
  - CHECK(role IN ('editor', 'user'))
```

### Миграции

- Автоматические через `Database.migrate_add_role_column()`
- Безопасное добавление столбцов с проверкой существования
- Выполняются при инициализации `Database.__init__()`

## Стандарты кодирования

### Основные принципы

- **SOLID**: Разделение ответственности
  - `handlers` → взаимодействие с пользователем
  - `services` → бизнес-логика
  - `database` → работа с данными
  - `config` → конфигурация
- **Async/Await**: Все IO операции асинхронные
- **Type Hints**: Обязательная типизация функций
- **Logging**: Структурированное логирование через `logger_config.py`

### Конвенции именования

- **Классы**: `PascalCase` (DataSynchronizer, AIComparator, AccessManager)
- **Функции**: `snake_case` (sync_three_columns, validate_with_ai)
- **Константы**: `UPPER_SNAKE_CASE` (FILE_CONFIGS, MANDATORY_MATCHES)
- **Private методы**: `_leading_underscore` (_validate_with_ai)

### Обработка ошибок

```python
try:
    # Основная логика
except SpecificException as e:
    logger.error(f"Описание ошибки: {e}", exc_info=True)
    # Graceful degradation
```

## Ключевые компоненты

### 1. Схемы сопоставлений (Schema)

- Сохраняются в БД с полным JSON результата AI в поле `full_comparison_json`
- Содержат:
  - **Тройные совпадения** (`matches_all_three`) - столбцы присутствуют во всех 3 файлах
  - **Парные совпадения**: `matches_1_2`, `matches_1_3`, `matches_2_3`
  - **Уникальные**: `only_in_first`, `only_in_second`, `only_in_third`
- **Confidence threshold**: 85% (совпадения ниже не сохраняются в `schema_matches`)
- **Формат**: JSON с сохранением в БД для полной совместимости

Изучил файл CLAUDE.MD. Вот что нужно дополнить с учетом новой доработки:
​

📍 Место 1: После раздела "### 1. Схемы сопоставлений (Schema)" (после строки про Confidence threshold)
Найди текст:

text
- **Confidence threshold**: 85% (совпадения ниже не сохраняются в `schema_matches`)
- **Формат**: JSON с сохранением в БД для полной совместимости
Добавь ПОСЛЕ этого:

text
### Работа с парными и тройными сопоставлениями (v2.1)

#### Просмотр сопоставлений
Функция `show_schema_matches` в `bot/handlers/schema_edit.py` отображает:
- **Тройные сопоставления** - все 3 маркетплейса (WB + Ozon + Яндекс)
- **Парные сопоставления**:
  - `matches_1_2` - WB + Ozon (Яндекс = N/A)
  - `matches_1_3` - WB + Яндекс (Ozon = N/A)
  - `matches_2_3` - Ozon + Яндекс (WB = N/A)

Формат вывода с явным указанием N/A для отсутствующих маркетплейсов.

#### Редактирование сопоставлений
Пользователь может:
1. **Редактировать любое сопоставление** (тройное или парное)
2. **Превращать парное в тройное** - заполнить N/A значением из шаблона
3. **Превращать тройное в парное** - ввести `NA` для удаления столбца

#### Логика автоперемещения между группами
При изменении столбца система автоматически определяет новый тип:
- Если заполнены все 3 столбца → перемещается в `matches_all_three`
- Если заполнены только 2 → перемещается в соответствующую парную группу
- Если остался 1 столбец → ошибка (минимум 2 маркетплейса)

**Вспомогательные функции** (`bot/handlers/schema_edit.py`):
- `_get_group_key(match_type)` - получить ключ БД для типа сопоставления
- `_format_type(match_type)` - форматирование типа для отображения пользователю

#### Фильтрация по типам (v2.1.1)

Функция `edit_action_selected` в `bot/handlers/schema_edit.py` обрабатывает выбор фильтра:
- **Кнопки фильтра**: `get_filter_matches_keyboard()` из `bot/keyboards.py`
- **Логика фильтрации**: Отображаются только сопоставления выбранного типа
- **Сохранение номеров**: Номера сопоставлений соответствуют индексам в `edit_all_matches`

**Преимущества:**
- Быстрый просмотр конкретного типа сопоставлений
- Не нужно прокручивать длинный список
- Удобно для схем с 50+ сопоставлениями

**Типы фильтров:**
- `🎯 Показать тройные` → `filter_type = 'triple'`
- `🔗 Показать парные (WB+Ozon)` → `filter_type = 'pair_1_2'`
- `🔗 Показать парные (WB+Яндекс)` → `filter_type = 'pair_1_3'`
- `🔗 Показать парные (Ozon+Яндекс)` → `filter_type = 'pair_2_3'`
- `📋 Показать всё` → `filter_type = None`

#### Специальный ввод для удаления
Ввод `NA` (без учета регистра) в поле столбца:
- Очищает значение столбца
- Автоматически переводит тройное → парное
- Показывает уведомление: `🔄 Тип изменен: Тройное → Парное (WB+Ozon)`

### 2. Валидация значений (5 уровней)

1. **Точное совпадение** (case-sensitive)
2. **Нормализованное совпадение** (lowercase + strip)
3. **Извлечение чисел** из строк (regex `\d+`)
4. **Subset matching** (совпадение по словам)
5. **AI-сопоставление** через OpenRouter (финальный уровень)

### 3. Множественные значения

- **Разделитель входной**: `;` (точка с запятой)
- **WB**: только первое значение
- **Ozon**: выход через `"; "` (точка с запятой + пробел)
- **Яндекс**: выход через `", "` (запятая + пробел)

### 4. Специальная обработка

- **Габариты**: композитный формат `"длина/ширина/высота"` для Яндекса
- **Единицы измерения**: автоконвертация (мм ↔ см)
- **Артикулы**: выравнивание по всем маркетплейсам
- **Фильтрация**: Максимум 50 символов в артикуле

## Формат данных

### Обязательные столбцы (MANDATORY_MATCHES)

```python
{
    "column_1": "Артикул продавца",   # WB
    "column_2": "Артикул*",            # Ozon
    "column_3": "Ваш SKU *",           # Яндекс
    "description": "Уникальный артикул товара"
}
```

Всего 10 обязательных сопоставлений (артикул, баркод, бренд, название, описание, вес, фото, высота, длина, ширина).

### Исключённые столбцы

- Цена (все маркетплейсы)
- НДС
- Rich-контент JSON
- Цена до скидки
- Старая цена

## AI Integration

### Настройка модели (.env)

```env
OPENROUTER_API_KEY=sk-or-v1-xxx
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
AI_MODEL=google/gemini-2.5-flash-preview-09-2025
AI_TEMPERATURE=0.1
```

### Два прохода AI

1. **Первый проход**: Сопоставление всех столбцов трёх файлов
2. **Второй проход**: Оставшиеся столбцы после первого прохода (если нужно)

### Логирование AI

- Все AI-валидации сохраняются в список `ai_validation_log`
- Экспортируются в отдельный лист Excel **"AI_Логи"**
- Формат: `[Артикул, Столбец, Исходное значение, Найденное значение, Confidence]`

## Конфигурация (.env)

```env
# OpenRouter API
OPENROUTER_API_KEY=sk-or-v1-xxx
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
AI_MODEL=google/gemini-2.5-flash-preview-09-2025
AI_TEMPERATURE=0.1

# Telegram Bot
TELEGRAM_BOT_TOKEN=xxx

# Права доступа
ACCESS_OWNER_ID=123456789          # Обязательно! ID владельца
ACCESS_ADMIN_ID=987654321          # Опционально (можно через БД)

# Прокси (опционально)
PROXY_ENABLED=false
PROXY_URL=http://user:pass@host:port
```

## Тестирование

### Ручное тестирование

1. Загрузка 3 файлов (WB, Ozon, Яндекс)
2. Создание схемы сопоставления
3. Проверка синхронизации по артикулу
4. Валидация значений из справочников

### Debug режим

```python
logger.setLevel(logging.DEBUG)  # Подробные логи в utils/logger_config.py
```

## Deployment

### Требования

```
Python 3.9+
aiogram==3.x
pandas
openpyxl
openai (для OpenRouter)
python-dotenv
```

### Запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python main.py
```

### Переменные окружения

Создайте файл `.env` с обязательными параметрами (см. раздел "Конфигурация").

## Roadmap & Known Issues

### TODO

- [x] Просмотр и редактирование парных сопоставлений (v2.1 - завершено)
- [x] Автоматическое перемещение между группами при изменении типа (v2.1 - завершено)
- [x] Фильтрация сопоставлений по типам (v2.1.1 - завершено)
- [ ] Миграция старых схем на новый формат JSON (если есть legacy)
- [ ] Добавление unit-тестов (pytest)
- [ ] CI/CD pipeline
- [ ] Поддержка 4-го маркетплейса (расширение архитектуры)

### Ограничения

- Максимум 50 символов в артикуле (фильтрация некорректных данных)
- Файлы только .xlsx формата
- OpenRouter API rate limits


## Важные предостережения

### НЕ изменять без обсуждения

- Формат обязательных столбцов (`MANDATORY_MATCHES`)
- Логику 5-уровневой валидации
- Разделители множественных значений (`;` → `"; "` / `", "`)
- Структуру прав доступа (роли и лимиты whitelist)

### Критичные зависимости

- Порядок вызовов: схема → синхронизация → валидация
- FSM states должны быть последовательными
- AI-логи обязательны для каждой валидации
- AccessControlMiddleware должен быть зарегистрирован ПЕРЕД handlers
- `ACCESS_OWNER_ID` в .env обязателен для работы системы доступа

## Типичные сценарии работы

### Добавление нового уровня валидации

1. Добавить метод в `DataSynchronizer` (`services/data_synchronizer.py`)
2. Вставить в цепочку валидации перед AI
3. Обновить формат AI_Логов в `excel_writer.py`

### Добавление нового маркетплейса

1. Расширить `FILE_CONFIGS` в `config/config.py`
2. Добавить `MANDATORY_MATCHES` для нового файла
3. Обновить логику `sync_three_columns` → `sync_four_columns`
4. Адаптировать `AIComparator` для 4 файлов

### Редактирование схем: парные ↔ тройные сопоставления

#### Просмотр сопоставлений с фильтрацией (v2.1.1)

1. Меню "📁 Схемы"
2. "✏️ Редактировать схему"
3. Выбрать схему для редактирования
4. Загрузить 3 актуальных шаблона маркетплейсов (WB, Ozon, Яндекс)

**После загрузки файлов отображается:**

✅ Файлы загружены!

📋 Схема: Моя схема
📊 Всего сопоставлений: 15

🎯 Тройные: 8
🔗 Парные (WB+Ozon): 3
🔗 Парные (WB+Яндекс): 2
🔗 Парные (Ozon+Яндекс): 2

Выбери тип для просмотра или начни редактирование:

[🎯 Показать тройные]
[🔗 Показать парные (WB+Ozon)]
[🔗 Показать парные (WB+Яндекс)]
[🔗 Показать парные (Ozon+Яндекс)]
[📋 Показать всё]
[✏️ Редактировать сопоставление]
[❌ Отмена]

**Фильтрация по типу:**
- Нажми на кнопку нужного типа → отобразятся только сопоставления этого типа
- Номера сопоставлений **глобальные** (из полного списка), не сбрасываются при фильтрации
- Можно переключаться между типами без перезагрузки файлов
- Кнопка "📋 Показать всё" → вернуться к полному списку

**Пример отфильтрованного списка:**

📋 Схема: Моя схема
🔗 Парные (WB + Ozon)
📊 Показано: 3

#9: Размер упаковки | Package Size | N/A
#10: Срок годности | Shelf Life | N/A
#11: Условия хранения | Storage | N/A

Выбери другой тип или начни редактирование:
[кнопки фильтра]

**Результат**: Отображение всех сопоставлений с группировкой:
- 🎯 Тройные сопоставления: X
- 🔗 Парные (WB + Ozon): Y
- 🔗 Парные (WB + Яндекс): Z
- 🔗 Парные (Ozon + Яндекс): W

#### Превращение парного в тройное
**Сценарий**: Есть парное сопоставление `Цвет | Color | N/A`, нужно добавить Яндекс.

1. Загрузить 3 актуальных шаблона маркетплейсов
2. Выбрать номер парного сопоставления
3. Нажать "📝 Изменить Яндекс столбец"
4. Ввести название или номер столбца из списка
5. Система автоматически переместит в `matches_all_three`

**Уведомление**:

✅ Сопоставление обновлено!

📋 Схема: Моя схема
📝 Столбец Яндекс:
Было: N/A
Стало: Цвет товара
🔄 Тип изменен: Парное (WB+Ozon) → Тройное

#### Превращение тройного в парное
**Сценарий**: Нужно убрать столбец Яндекса из тройного сопоставления.

1. Загрузить 3 актуальных шаблона маркетплейсов
2. Выбрать номер тройного сопоставления
3. Нажать "📝 Изменить Яндекс столбец"
4. Ввести: `NA` (или `na`, `Na`)
5. Система автоматически переместит в `matches_1_2` (WB+Ozon)

**Уведомление**:

✅ Сопоставление обновлено!

📋 Схема: Моя схема
📝 Столбец Яндекс:
Было: Цвет товара
Стало: N/A
🔄 Тип изменен: Тройное → Парное (WB+Ozon)

#### Изменение столбца без смены типа
Если меняешь столбец, но количество маркетплейсов остается тем же:
- Тип не изменяется
- Обновляется только значение в той же группе
- Уведомление без строки "🔄 Тип изменен"

### Управление пользователями

#### Добавление администратора (владелец)

1. Меню "🔐 Управление доступами"
2. "✏️ Изменить администратора"
3. Ввести Telegram ID

#### Добавление пользователя в whitelist (владелец/админ)

1. Меню "🔐 Управление доступами"
2. "📋 Белый список пользователей"
3. "➕ Добавить пользователя"
4. Выбрать роль (Редактор/Пользователь)
5. Ввести Telegram ID

#### Удаление из whitelist

1. Меню "🔐 Управление доступами"
2. "📋 Белый список пользователей"
3. "➖ Удалить пользователя"
4. Выбрать ID из списка

## Middleware и безопасность

### AccessControlMiddleware

Регистрируется для всех `message` и `callback_query` событий:

```python
dp.message.middleware(AccessControlMiddleware())
dp.callback_query.middleware(AccessControlMiddleware())
```

- Проверяет `user_id` перед каждым обработчиком
- Блокирует неавторизованных пользователей
- Отправляет сообщение "⛔ У вас нет доступа"

### Иерархия проверок

1. Middleware → `AccessManager.has_access(user_id)`
2. Handler → дополнительные проверки через `AccessManager.is_owner()`, `is_admin()`, etc.
3. Database → ограничения на уровне SQL (UNIQUE, CHECK constraints)

## Логирование

### Конфигурация

Модуль `utils/logger_config.py` настраивает:
- Формат: `[%(asctime)s] [%(name)s] [%(levelname)s] %(message)s`
- Ротация файлов (если настроено)
- Вывод в консоль + файл

### Использование

```python
from utils.logger_config import setup_logger
logger = setup_logger('module_name')

logger.info("Информационное сообщение")
logger.error("Ошибка", exc_info=True)
logger.debug("Детальная информация для отладки")
```

## Обработка файлов Excel

### Чтение (utils/excel_reader.py)

```python
from utils.excel_reader import read_excel_file
df, headers = read_excel_file(filepath, marketplace='wildberries')
```

### Запись (utils/excel_writer.py)

- Создаёт 3 листа: WB, Ozon, Яндекс
- Добавляет лист "AI_Логи" с детализацией всех AI-валидаций
- Форматирование (заливка заголовков, выравнивание)
- Auto-width для колонок

## Примеры команд бота

### Основные команды

- `/start` - Запуск бота и главное меню
- `/cancel` - Отмена текущей операции

### Меню (для всех авторизованных)

- 📤 Загрузить файлы - Начало процесса синхронизации
- 📁 Схемы - Управление схемами сопоставлений
- 📊 Статистика - Просмотр своей статистики

### Меню (владелец + админ)

- 🔐 Управление доступами - Управление администратором и whitelist

## FAQ

### Как узнать Telegram ID пользователя?

Попросить пользователя написать боту `@userinfobot` и прислать полученный ID.

### Что делать если все whitelist слоты заняты?

Сначала удалить одного пользователя, затем добавить нового.

### Можно ли изменить роль существующего пользователя?

Нет, нужно сначала удалить из whitelist, затем добавить заново с новой ролью.

### Где хранится администратор?

Приоритет: БД (`system_settings` таблица) → `.env` (`ACCESS_ADMIN_ID`).

### Как удалить столбец из тройного сопоставления?

При редактировании сопоставления введи `NA` (без учета регистра) в поле столбца, который хочешь удалить. Сопоставление автоматически станет парным.

### Почему парные сопоставления не отображались раньше?

В предыдущих версиях (до 2.1) отображались только тройные сопоставления из `matches_all_three`. 

**История изменений:**
- **v2.1** - Добавлен просмотр и редактирование парных сопоставлений
- **v2.1.1** - Добавлена фильтрация по типам для удобства работы с большими схемами

### Как работает фильтрация сопоставлений?

После загрузки 3 шаблонов отображается меню с кнопками фильтра. Выбери тип сопоставлений (тройные/парные) → увидишь только их. Номера сопоставлений сохраняются из полного списка для корректного редактирования.

### Что произойдет, если ввести NA для единственного заполненного столбца?

Система выдаст ошибку: "❌ Сопоставление должно содержать минимум 2 маркетплейса!". Нельзя создать сопоставление с одним столбцом.

---

**Версия документации**: 2.1.1  
**Дата обновления**: Январь 2026  
**Последние изменения**: 
- v2.1.1: Добавлена фильтрация сопоставлений по типам (тройные/парные) для удобного просмотра больших схем
- v2.1: Поддержка просмотра и редактирования парных сопоставлений с автоматическим перемещением между группами
**Автор проекта**: Александр (@your_username)
