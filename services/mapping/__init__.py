"""
Пакет AI-агента маппинга для интеграции с PIM+FDM.

Отвечает за обработку внешних заданий, поступающих через
REST API /v1/mapping-tasks:

    - models.py               — модели данных задания (dataclasses)
    - validators.py           — ручная валидация входящего JSON
    - attribute_mapper.py     — стратегия маппинга атрибутов схемы
    - reference_value_mapper.py — стратегия маппинга справочных значений
    - job_worker.py           — фоновый воркер заданий (жизненный цикл)

Пакет НЕ экспортирует модули на верхнем уровне: импорт выполняется
напрямую (from services.mapping.models import ...). Это исключает
циклические зависимости и позволяет пакету импортироваться частично.

AI-запросы выполняются через общий AIComparator приложения —
глобальный семафор ограничивает суммарную нагрузку на LLM-провайдера
(общую для синхронизации файлов и маппинга).
"""

__all__: list[str] = []
